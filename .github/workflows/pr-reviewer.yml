# ─────────────────────────────────────────────────────────
# AUTO-GENERATED by: gh aw compile .github/agents/pr-reviewer.md
# Do NOT edit this file directly. Edit the source markdown instead.
# ─────────────────────────────────────────────────────────
name: "Agentic: PR Reviewer"

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  pull-requests: write
  contents: read

jobs:
  review:
    name: AI PR Review
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for diff analysis

      - name: Run PR Reviewer Agent
        uses: github/agentic-workflows@v1
        with:
          agent-source: .github/agents/pr-reviewer.md
          github-token: ${{ secrets.GITHUB_TOKEN }}
          ai-engine: copilot
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
